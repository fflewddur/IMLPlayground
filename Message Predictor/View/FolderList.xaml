<UserControl x:Class="MessagePredictor.FolderList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="200">
    <Grid>
        <ListView SelectionMode="Single" ItemsSource="{Binding Folders}" SelectedItem="{Binding CurrentFolder}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical">
                        <Label Content="{Binding Label}" Style="{StaticResource headerStyle2}" />
                        <Label Content="{Binding Count}" ContentStringFormat="Items: {0}" Padding="20 2 2 2"/>
                        <Label Content="{Binding CorrectPredictions}" ContentStringFormat="Correct: {0}" Padding="20 2 2 2"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
