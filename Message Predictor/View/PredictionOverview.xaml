<UserControl x:Class="MessagePredictor.View.PredictionOverview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        
        <!-- Labels -->
        <Label Grid.Row="0" Grid.Column="0" Content="{Binding Topic1UserLabel}"/>
        <Label Grid.Row="1" Grid.Column="0" Content="{Binding Topic2UserLabel}"/>
        <Label Grid.Row="2" Grid.Column="0" Content="Recently changed"/>
        
        <!-- Values -->
        <Label Grid.Row="0" Grid.Column="1" Content="{Binding Topic1Predictions}"/>
        <Label Grid.Row="1" Grid.Column="1" Content="{Binding Topic2Predictions}"/>
        <Label Grid.Row="2" Grid.Column="1" Content="{Binding RecentlyChangedPredictions}"/>
    </Grid>
</UserControl>
