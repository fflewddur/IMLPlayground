<UserControl x:Class="MessagePredictor.View.PredictionExplanation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:conv="clr-namespace:MessagePredictor.Converters" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0">
            <Label.Content>
                <MultiBinding>
                    <MultiBinding.Converter>
                        <conv:DictionaryItemFromLabelConverter/>
                    </MultiBinding.Converter>

                    <Binding Path="FolderListVM.SelectedFolder.SelectedMessage.Prediction.EvidencePerClass"/>
                    <Binding Path="Labels[0]"/>
                </MultiBinding>
            </Label.Content>
        </Label>

        <Label Grid.Row="1">
            <Label.Content>
                <MultiBinding>
                    <MultiBinding.Converter>
                        <conv:DictionaryItemFromLabelConverter/>
                    </MultiBinding.Converter>

                    <Binding Path="FolderListVM.SelectedFolder.SelectedMessage.Prediction.EvidencePerClass"/>
                    <Binding Path="Labels[1]"/>
                </MultiBinding>
            </Label.Content>
        </Label>

        <Label Grid.Row="2" Content="{Binding Labels[0]}"/>
        <Label Grid.Row="3" Content="{Binding Labels[1]}"/>
    </Grid>
</UserControl>
