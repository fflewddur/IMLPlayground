<UserControl x:Class="MessagePredictor.View.PredictionExplanation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:conv="clr-namespace:MessagePredictor.Converters" 
             xmlns:local="clr-namespace:MessagePredictor.View" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="{Binding FolderListVM.SelectedFolder.SelectedMessage.Prediction.ImportantWordDesc, Mode=OneWay}" TextWrapping="Wrap" Margin="5" />

        <local:EvidenceGraph Grid.Row="1" Label="{Binding Labels[0]}" GraphColorUser="#FFDC9717" GraphColorSystem="#FFB66400">
            <local:EvidenceGraph.Evidence>
                <MultiBinding>
                    <MultiBinding.Converter>
                        <conv:DictionaryItemFromLabelConverter/>
                    </MultiBinding.Converter>
                    <Binding Path="FolderListVM.SelectedFolder.SelectedMessage.Prediction.EvidencePerClass"/>
                    <Binding Path="Labels[0]"/>
                </MultiBinding>
            </local:EvidenceGraph.Evidence>
        </local:EvidenceGraph>

        <local:EvidenceGraph Grid.Row="2" Label="{Binding Labels[1]}" GraphColorUser="#FF2EBB2E" GraphColorSystem="#FF127812">
            <local:EvidenceGraph.Evidence>
                <MultiBinding>
                    <MultiBinding.Converter>
                        <conv:DictionaryItemFromLabelConverter/>
                    </MultiBinding.Converter>
                    <Binding Path="FolderListVM.SelectedFolder.SelectedMessage.Prediction.EvidencePerClass"/>
                    <Binding Path="Labels[1]"/>
                </MultiBinding>
            </local:EvidenceGraph.Evidence>
        </local:EvidenceGraph>
        
        <Label Grid.Row="3" Content="Class imbalance explanation will go here."/>
        <Label Grid.Row="4" Content="Pie chart will go here."/>
    </Grid>
</UserControl>
