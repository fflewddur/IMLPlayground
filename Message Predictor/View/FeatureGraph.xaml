<UserControl x:Class="MessagePredictor.View.FeatureGraph"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Name="root"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <ItemsControl Grid.Column="0" Name="FeatureControl" ItemsSource="{Binding ItemsSource,ElementName=root}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="10" PreviewMouseMove="Rectangle_PreviewMouseMove" PreviewMouseLeftButtonUp="Rectangle_PreviewMouseLeftButtonUp" Background="Transparent">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Rectangle Grid.Row="0" Width="20" Height="{Binding UserHeight}" Fill="LightGreen" VerticalAlignment="Bottom" Cursor="SizeNS" 
                                   PreviewMouseLeftButtonDown="Rectangle_PreviewMouseLeftButtonDown" />
                        <Rectangle Grid.Row="1" Width="20" Height="{Binding Height}" Fill="DarkGreen" />
                        <TextBlock Grid.Row="2" Text="{Binding Characters, Mode=OneWay}" Margin="0 20 0 0"/>
                    </Grid>
                    
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        
        <!-- Unused weight -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Rectangle Grid.Row="0" Width="20" Height="{Binding UserHeight}" Fill="LightGreen" VerticalAlignment="Bottom" Cursor="SizeNS" 
                                   PreviewMouseLeftButtonDown="Rectangle_PreviewMouseLeftButtonDown" />
            <Rectangle Grid.Row="1" Width="20" Height="{Binding Height}" Fill="DarkGreen" />
            <TextBlock Grid.Row="2" Text="{Binding Characters, Mode=OneWay}" Margin="0 20 0 0"/>
        </Grid>
        
        <Button Grid.Column="2" VerticalAlignment="Center">Add importance</Button>
    </Grid>
</UserControl>
